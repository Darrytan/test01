<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <!--注意：只配置和dao相关的-->

    <!--扫描dao层注解-->
    <context:component-scan base-package="com.itheima.dao"></context:component-scan>



    <!--读取properties配置文件-->
    <context:property-placeholder location="classpath:db.properties"/>

    <!--数据源-->
    <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
        <property name="driverClassName" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <!--配置SqlSessionFactory-->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <!--引用数据源-->
        <property name="dataSource" ref="dataSource"/>
        <!-- 扫描sql配置文件:mapper需要的xml文件 -->
        <property name="mapperLocations" value="classpath:com/itheima/mapper/*Mapper.xml"/>

        <!--分页  配置pageHelper拦截器-->
        <property name="plugins">
            <array>
                <bean class="com.github.pagehelper.PageInterceptor">
                    <property name="properties">
                        <props>
                            <!--数据库类型-->
                            <prop key="helperDialect">oracle</prop> <!--自动检测当前数据库链接.指定分页插件使用哪种方言,分页的sql不用写-->
                            <prop key="reasonable">true</prop>  <!--分页合理化参数,默认值未false,设置true,pageNum<=0会查询第一页,超过总数时,会查询最后一页-->
                        </props>
                    </property>
                </bean>
            </array>
        </property>
    </bean>

    <!--配置自动扫描mapper-->   <!-- 4.配置扫描Dao接口包，动态实现Dao接口，注入到spring容器中 -->
    <bean id="mapperScannerConfigurer" class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <!--配置mapper所在的位置
                原则:mapper.xml要和dao接口同名且在同一包下
            -->
        <property name="basePackage" value="com.itheima.dao"/>
    </bean>
</beans>